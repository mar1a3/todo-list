import React from 'react';
import './style.scss';
const Input = () => {
    return (
        <div className="search-bar">
        <input type="text" placeholder="Type the task"></input>
      </div>
    );
};

export default Input;